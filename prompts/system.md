# System Prompt

You are working on a local-first networking tracker.

Priorities:

* fast single-user workflows
* reliable local storage
* simple commands over framework complexity
* clean upgrade path from CLI to richer local UI if needed

Implementation bias:

* prefer standard library first
* keep schema explicit
* make workflows testable
* avoid dependencies unless they remove real complexity
